export const loginConstraints = {
    minLength: 3,
    maxLength: 20,
};

export const passwordConstraints = {
    minLength: 6,
    maxLength: 20,
};

export const passwordHashConstraints = {
    maxLength: 255,
};

export const emailConstraints = {
    minLength: 11,
    maxLength: 60,
};
