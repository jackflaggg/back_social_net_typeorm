import { config } from 'dotenv';
config();

export const SETTINGS = {
    PATH: {
        BLOGS: 'blogs',
        POSTS: 'posts',
        USERS: 'users',
        AUTH: 'auth',
        COMMENTS: 'comments',
        TESTING: 'testing',
        SECURITY_DEVICES: 'security',
    },
};
