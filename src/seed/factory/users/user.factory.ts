// define(User, (faker: typeof Faker) => {
//     const user = new User();
//     user.login = faker.internet.userName();
//     user.email = faker.internet.email();
//     user.passwordHash = faker.internet.password(); // Замените на хэширование пароля, если необходимо
//     user.sentEmailRegistration = false; // Или любой другой логический параметр по умолчанию
//     return user;
// });
